"""
Database models for the vault_ui application.

The vault_ui app provides the frontend interface layer of the
IdentityVault system. It does not define its own database models,
as it relies on models from the profiles and connections apps.
"""
from django.db import models

# Create your models here.
