"""
Admin configuration for the vault_ui application.

The vault_ui app provides the user interface layer of the system
and does not define database models that require Django admin
registration.
"""
from django.contrib import admin

# Register your models here.
