"""
Application configuration for the connections module.

Registers the connections app within the Django project.
"""
from django.apps import AppConfig


class ConnectionsConfig(AppConfig):
    """Configuration class for the connections application."""
    name = 'connections'
